## Session 1 — 2026-03-08

### Task: Phase 1 — Backend complet (Node.js + Express + Socket.io)

**Réalisé :**
- Initialisé `/server` avec Node.js, Express, Socket.io, cors, nodemon, jest
- `src/deck.js` : création du jeu 52 cartes, shuffle Fisher-Yates, deal 26/26
- `src/game.js` : état complet du jeu (phases, piles, pot, battlePot), comparaison de cartes, logique de bataille récursive, détection fin de partie, sérialisation de l'état
- `index.js` : serveur Express + Socket.io avec événements `create_room`, `join_room`, `player_ready`, `game_state`, `game_over`, gestion déconnexion
- Tests Jest : 18 tests passants (deck + game logic)

**Tests :** 18/18 ✓

## Session 2 — 2026-03-08

### Task: Phase 2 — Initialisation client React + Vite

**Réalisé :**
- Initialisé `/client` avec Vite + React template
- Installé Tailwind CSS v4 (`@tailwindcss/vite`), Framer Motion, Socket.io-client
- Configuré le plugin Tailwind dans `vite.config.js`
- Remplacé `index.css` par `@import "tailwindcss"` (Tailwind v4 style)
- Nettoyé le boilerplate Vite (App.css, react.svg, compteur)
- `App.jsx` minimal vérifiant que Tailwind fonctionne (fond vert, texte blanc)

**Build :** `vite build` ✓ (29 modules, 0 erreurs)

## Session 3 — 2026-03-08

### Task: Phase 2 — Frontend structure and Socket.io connection

**Réalisé :**
- `src/socket.js` : singleton Socket.io-client (autoConnect: false)
- `src/context/GameContext.jsx` : React Context + useReducer, gère tous les événements socket (`room_created`, `game_state`, `game_over`, `player_ready_ack`, `error`), expose `createRoom`, `joinRoom`, `flipCard`, `reset`
- `src/screens/HomeScreen.jsx` : écran d'accueil avec saisie du nom, bouton "Créer une partie", champ code + bouton "Rejoindre", affichage d'erreur animé
- `src/screens/WaitingScreen.jsx` : écran d'attente animé (Framer Motion), affichage du code de room, copie en un clic via clipboard API
- `src/screens/GameScreen.jsx` : placeholder Phase 3 + écran de fin (victoire/défaite)
- `src/App.jsx` : routing basé sur `state.screen` (home → waiting → game/over), wrappé dans `<GameProvider>`

**Build :** `vite build` ✓ (462 modules, 0 erreurs)

## Session 4 — 2026-03-08

### Task: Phase 3 — Table de jeu

**Réalisé :**
- `src/components/Card.jsx` : composant carte avec face visible (valeur + suit + couleur rouge/noir) et face cachée (dos bleu décoratif), prop `highlight` pour bordure dorée du vainqueur
- `src/components/GameTable.jsx` : layout vertical complet — adversaire (haut), zone centrale (milieu), joueur local (bas). Affiche le résultat du dernier tour (cartes face visible, "🏆" et label du vainqueur, "⚔ BATAILLE !" si applicable, +N cartes si bataille). Indicateur animé "Prêt !" quand l'adversaire a cliqué. Bouton "Retourner" désactivé si déjà cliqué ce tour.
- `src/context/GameContext.jsx` : ajout de `lastResult` (card1, card2, winnerId, cardsWon, event) stocké après chaque tour résolu, `myFlipped` (true après flipCard, false après game_state), passage de card1/card2/winnerId/cardsWon dans l'action GAME_STATE
- `src/screens/GameScreen.jsx` : remplace le placeholder par `<GameTable />`, conserve l'écran de fin (victoire/défaite)

**Build :** `vite build` ✓ (464 modules, 0 erreurs)

## Session 5 — 2026-03-08

### Task: Phase 4 — Animation de distribution initiale

**Réalisé :**
- `src/components/GameTable.jsx` : ajout du composant `DealAnimation` (Framer Motion) — 10 cartes volent alternativement vers la pile adversaire (haut) et la pile joueur (bas) avec délai progressif (0.13 s entre chaque)
- Overlay semi-transparent `bg-green-950/70` + texte "Distribution en cours…" pendant l'animation
- Animation se termine automatiquement après 1.9 s via `setTimeout`, `AnimatePresence` gère le fade-out
- `GameTable` utilise `useState(true)` pour `isDealing`, `useCallback` pour `handleDealDone`

**Build :** `vite build` ✓ (464 modules, 0 erreurs)

## Session 6 — 2026-03-08

### Task: Phase 4 — Animation de retournement de carte (flip 3D)

**Réalisé :**
- `src/components/Card.jsx` : ajout du composant `FlippingCard` (export nommé) — flip 3D via Framer Motion (`rotateY: 0 → 180`) avec `perspective: 600px`, `transformStyle: preserve-3d`, `backfaceVisibility: hidden` sur les deux faces (dos bleu + face visible)
- `src/components/GameTable.jsx` : import de `FlippingCard`, ajout du state `flipKey` incrémenté à chaque changement de `lastResult` (via `useEffect`), remplacement de `<Card>` par `<FlippingCard key={flipKey}>` dans la zone centrale (léger délai de 0.1 s sur la carte du joueur local)

**Build :** `vite build` ✓ (464 modules, 0 erreurs)

## Session 7 — 2026-03-08

### Task: Phase 4 — Animation de déplacement (slide-to-winner)

**Réalisé :**
- `src/components/GameTable.jsx` : ajout du state `slideDir` (0 / -1 / +1) — après 750 ms (flip terminé), les cartes du centre glissent vers la pile du vainqueur via un `motion.div` wrapper (`y: ±350, opacity: 0, scale: 0.6`, durée 0.45 s). Le slideDir est réinitialisé à 0 à chaque nouveau `lastResult`. Direction : +1 (vers bas / ma pile) si je gagne, -1 (vers haut / pile adversaire) si l'adversaire gagne.

**Build :** `vite build` ✓ (464 modules, 0 erreurs)

## Session 8 — 2026-03-08

### Task: Phase 4 — Animation de bataille (cartes cachées)

**Réalisé :**
- `src/components/GameTable.jsx` : ajout du composant `BattlePlacingCards` — 6 cartes face cachée apparaissent une par une depuis le haut (adversaire) et le bas (joueur), avec un délai de 0.28 s entre chaque et une animation `backOut` (légèrement rebondissante). Rotations légèrement prédéfinies pour éviter le re-render.
- Fusionné les deux `useEffect` (flipKey + slideDir) en un seul effet séquencé : pour un tour de bataille, l'animation de pose (2 000 ms) précède le flip des cartes de résolution, lui-même suivi du slide (750 ms). Pour un tour normal, le comportement est identique à avant.
- La mention "⚔ BATAILLE !" reste visible pendant toute la séquence (placing + résolution).
- Centre de jeu restructuré avec `<AnimatePresence mode="wait">` pour alterner entre `BattlePlacingCards`, les cartes de résolution, et l'état idle.

**Build :** `vite build` ✓ (464 modules, 0 erreurs)
**Tests :** 18/18 ✓

## Session 9 — 2026-03-08

### Task: Phase 4 — Effet visuel "Bataille !" (flash + texte animé)

**Réalisé :**
- `src/components/GameTable.jsx` : ajout du composant `BattleFlash` — overlay rouge semi-transparent (`bg-red-600/30`) + grand texte "⚔ BATAILLE !" (text-6xl, font-black, glow jaune/orange via `textShadow`) qui surgit avec scale 0.1→1.5→1.15 et rotation, via Framer Motion `backOut`.
- Ajout du state `showBattleAlert` : déclenché à `true` au début de chaque bataille, auto-dismissed après 900 ms via `setTimeout`.
- Rendu via `<AnimatePresence>` avec exit fade-out fluide.
- Réorganisation du tableau `timers` dans l'useEffect pour regrouper tous les timers proprement.

**Build :** `vite build` ✓ (464 modules, 0 erreurs)

## Session 10 — 2026-03-08

### Task: Phase 4 — Animation de fin de partie (confettis + effet victoire/défaite)

**Réalisé :**
- `src/screens/GameScreen.jsx` : remplacement de l'écran de fin statique par une version animée Framer Motion.
  - **Victoire** : 40 pièces de confetti (couleurs variées, tailles différentes, délais et rotations prédéfinis) tombent en boucle via `motion.div` avec `repeat: Infinity`. La carte de résultat apparaît avec scale 0.5→1 + `backOut`. L'emoji 🏆 surgit avec scale 0→1.4→1 + rotation. Titre en `text-yellow-300`.
  - **Défaite** : overlay noir semi-transparent (`bg-black/40`) s'estompe en fondu. L'emoji 💀 surgit avec la même animation. Entrée animée du panneau identique.
  - Bouton "Retour à l'accueil" apparaît en fondu avec délai de 0.95 s.

**Build :** `vite build` ✓ (464 modules, 0 erreurs)
**Tests :** 18/18 ✓

## Session 11 — 2026-03-08

### Task: Phase 4 — Indicateur visuel quand l'adversaire a cliqué "Retourner"

**Réalisé :**
- `src/components/GameTable.jsx` : la carte face cachée de l'adversaire est wrappée dans un `motion.div`. Quand `opponentReady` est `true`, la carte tremble en boucle via une animation `rotateZ` keyframes (`[0, -6, 6, -5, 5, -3, 3, 0]`, durée 0.5 s, répétée toutes les 1.3 s). Quand `opponentReady` repasse à `false`, la rotation revient à 0. Le badge de ping jaune existant est conservé.

**Build :** `vite build` ✓ (464 modules, 0 erreurs)
**Tests :** 18/18 ✓

## Session 12 — 2026-03-08

### Task: Phase 5 — Noms des joueurs (saisi à l'entrée, affiché sur la table)

**Réalisé :**
- La saisie du nom et la transmission côté serveur étaient déjà en place (HomeScreen, GameContext, game.js, serializeState).
- `src/components/GameTable.jsx` : correction du label de la carte du joueur dans la zone centrale — `"Vous"` (hardcodé) remplacé par `me?.name ?? 'Vous'`, cohérent avec le reste de la table.
- `src/screens/WaitingScreen.jsx` : ajout d'un message de bienvenue personnalisé `"Bonjour, {myName} !"` (en jaune, visible uniquement si le nom est défini) au-dessus du texte d'attente.

**Build :** `vite build` ✓ (464 modules, 0 erreurs)
**Tests :** 18/18 ✓

## Session 14 — 2026-03-08

### Task: Phase 5 — Message de résultat clair sur les cartes avant le slide

**Réalisé :**
- `src/components/GameTable.jsx` : ajout du state `showResultBadge` — après la fin du flip (~700 ms), un badge animé apparaît en overlay sur chaque carte : "GAGNÉ !" (jaune avec glow) sur la carte gagnante, "PERDU" (gris semi-transparent) sur la carte perdante.
- Le badge est rendu via `<AnimatePresence>` avec animation `scale 0.5→1` + `backOut` pour l'entrée, fade-out à la sortie.
- Le délai du slide est étendu de 750 ms à 1500 ms (constante `SLIDE_DELAY`) pour laisser ~800 ms de visibilité au badge après le flip.
- `showResultBadge` est réinitialisé à `false` au début de chaque nouveau `lastResult` et quand le slide commence.

**Build :** `vite build` ✓ (464 modules, 0 erreurs)
**Tests :** 18/18 ✓

## Session 13 — 2026-03-08

### Task: Phase 5 — Historique des 5 derniers tours (bandeau latéral)

**Réalisé :**
- `src/context/GameContext.jsx` : ajout de `history: []` et `turnCount: 0` à l'état initial. Dans le reducer `GAME_STATE`, quand un résultat est présent (card1 défini), on incrémente `turnCount` et on pousse `{ turn, event, winnerId, cardsWon }` dans `history`, en gardant les 5 dernières entrées (`slice(-5)`).
- `src/components/GameTable.jsx` : ajout du composant `TurnHistory` — panneau absolu positionné en haut à droite, affiche les 5 derniers tours dans l'ordre anti-chronologique. Chaque entrée montre le numéro de tour, l'icône ⚔ si bataille, le nom du vainqueur (coloré en jaune si c'est moi, bleu sinon), et le nombre de cartes gagnées. Les entrées plus anciennes sont légèrement transparentes (opacity dégressive). Apparition animée via Framer Motion.

**Build :** `vite build` ✓ (464 modules, 0 erreurs)
**Tests :** 18/18 ✓

## Session 15 — 2026-03-08

### Task: Phase 5 — Responsive design (mobile + desktop)

**Réalisé :**
- `client/index.html` : titre corrigé de "client" → "Bataille"
- `src/components/GameTable.jsx` :
  - `TurnHistory` : `hidden md:flex` — panneau masqué sur mobile (évite le chevauchement avec le contenu)
  - `BattlePlacingCards` : cartes `w-10 h-14 sm:w-14 sm:h-20`, gap `gap-1 sm:gap-1.5` — évite le débordement horizontal sur mobile (était 366px > 343px disponibles)
  - `BattleFlash` : texte `text-4xl sm:text-6xl`
  - Zone centrale VS : gap `gap-3 sm:gap-6`
  - Bouton "Retourner" : padding `px-8 sm:px-10`, ajout de `touch-manipulation` pour éviter le double-tap zoom iOS
- `src/screens/GameScreen.jsx` : panneau fin de partie `p-6 sm:p-10`
- `src/screens/WaitingScreen.jsx` : code de room `text-3xl sm:text-5xl`

**Build :** `vite build` ✓ (464 modules, 0 erreurs)
**Tests :** 18/18 ✓

## Session 16 — 2026-03-08

### Task: Phase 5 — Bouton "Rejouer" (même room)

**Réalisé :**
- `server/src/game.js` : ajout de `restartVotes: new Set()` dans `createGame` et export de `restartGame(game)` — re-deal un nouveau jeu de 52 cartes, réinitialise piles, pot, battlePot, readyPlayers, restartVotes, winner, phase → 'playing'
- `server/index.js` : import de `restartGame`, ajout de l'événement `restart_game` — chaque joueur vote, quand les 2 ont voté → `restartGame` puis broadcast `game_state` avec event `'game_restarted'`; broadcast intermédiaire `restart_vote` pour notifier l'adversaire
- `src/context/GameContext.jsx` : ajout de `restartVoted` et `opponentRestartVoted` à l'état initial ; actions `RESTART_VOTED` et `OPPONENT_RESTART_VOTED` ; écoute de `restart_vote` socket ; `restartGame()` émet l'événement et dispatch `RESTART_VOTED` ; le reducer `GAME_STATE` détecte `game_restarted` et remet `history`, `turnCount`, `lastResult` à zéro
- `src/screens/GameScreen.jsx` : ajout du bouton "Rejouer" (vert) sur l'écran de fin — après le clic, remplacé par "En attente de l'adversaire…" (ou "Redémarrage…" si l'adversaire a aussi voté) ; bouton "Retour à l'accueil" toujours visible

**Build :** `vite build` ✓ (464 modules, 0 erreurs)
**Tests :** 18/18 ✓

## Session 17 — 2026-03-08

### Task: Phase 5 — README.md avec instructions de lancement

**Réalisé :**
- `README.md` : créé à la racine du projet avec stack technique, prérequis, instructions de lancement en 2 étapes (serveur sur :3001, client sur :5173), instructions de jeu (créer/rejoindre une room), et commande `npm test` pour les tests backend.

**Tests :** 18/18 ✓

## Session 18 — 2026-03-09

### Task: Phase 2 — Refonte UI HomeScreen.jsx

**Réalisé :**
- `src/screens/HomeScreen.jsx` : refonte complète du visuel — fond `bg-gray-950`, layout `flex-col gap-4` sans carte/cadre. Ordre exact : 1) champ "Ton pseudo" (max 20 car., boutons désactivés si vide), 2) bouton "Créer une partie", 3) séparateur "ou" centré, 4) champ "Code de la partie" + bouton "Rejoindre" sur la même ligne. Pre-remplissage automatique du code si `?room=` présent dans l'URL. Toute la logique existante conservée.

**Build :** `vite build` ✓ (464 modules, 0 erreurs)
**Tests :** 18/18 ✓

## Session 19 — 2026-03-09

### Task: Phase 6 — Servir les fichiers statiques du client depuis Express en production

**Réalisé :**
- `server/index.js` : en mode `NODE_ENV=production`, Express sert les fichiers statiques depuis `../client/dist` et ajoute un fallback SPA (`app.get('*', ...)` → `index.html`). En développement, le comportement CORS existant est conservé. Port par défaut : 3000 en production, 3001 en dev.
- `client/src/socket.js` : la socket se connecte à `''` (même origin) en production (`import.meta.env.PROD`) et à `http://localhost:3001` en développement.

**Build :** `vite build` ✓ (464 modules, 0 erreurs)
**Tests :** 18/18 ✓
